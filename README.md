# Mi-casa-tio
With this repository, we are going to buid a domotic house.

# Instalar dependencias (pip install)
pip install esptool adafruit-ampy

# Pip list
Para mostrar módulos instalación en el ambiente

# pip freeze para actualizar mi requeriments.txt
pip freeze > requeriments.txt

# Instalar requirements a mi ambiente
pip install -r .\requirements.txt